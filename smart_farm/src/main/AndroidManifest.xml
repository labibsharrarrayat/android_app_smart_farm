<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mp_graph"></activity>
        <activity android:name=".graphing" />
        <activity android:name=".Cucumber_4" />
        <activity android:name=".trying" />
        <activity android:name=".Contents" />
        <activity android:name=".Cucumber_3" />
        <activity android:name=".Cucumber_2" />
        <activity android:name=".Cucumber_1" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>